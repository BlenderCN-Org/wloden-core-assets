import fragment;
import procedural;

code_block(fragment) main
{
	let height = heightFieldFunction(FragmentInput.texcoord.x, FragmentInput.texcoord.y);

    FragmentOutput.color = float4(height, height, height, 1.0f);
}

