import environment;

semantic FragmentInput {
    in(0) texcoord: float2;
}

semantic FragmentOutput {
    out(0) color: float4;
}

uniform(binding = Source.Texture) mainTexture : texture2D;
uniform(binding = Sampler.Sampler) mainSampler : sampler;
